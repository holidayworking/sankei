source 'https://rubygems.org'

gem 'data_mapper'
gem 'feed-normalizer'
gem 'nokogiri'

group :mysql do
    gem 'dm-mysql-adapter'
end

group :postgresql do
    gem 'dm-postgres-adapter'
end

group :sqlite do
    gem 'dm-sqlite-adapter'
end
